/**
 * Renders the initial Phase 1 landing page used by smoke tests.
 *
 * @returns A static landing page for the web scaffold.
 */
export default function HomePage() {
  return (
    <main>
      <h1>Personal Context Web</h1>
      <p>
        Phase 1 scaffold is active. Database-backed features are intentionally
        deferred.
      </p>
    </main>
  );
}
